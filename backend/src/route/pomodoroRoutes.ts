import { Router } from 'express';
import { Database } from 'sqlite';
import { withErrorHandler } from '../server';

// using ws for real time update
export default function pomodoroRoutes(db: Database) {
  const router = Router();

  // update to field

  return router;
}
